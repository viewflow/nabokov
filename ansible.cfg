# Picked up by ansible-playbook when run from the repo root (see Makefile).
[defaults]
# The nabokov system user has no home directory, so keep remote temp under /tmp
# instead of ~/.ansible/tmp (silences the "Unable to use ... as temporary
# directory" warning on become).
remote_tmp = /tmp/.ansible-${USER}/tmp
