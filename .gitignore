# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# uv / virtualenv
.venv/

# caches
.ruff_cache/
.pytest_cache/
.mypy_cache/
.pyright/

# OS
.DS_Store

# local calibration corpus (copyrighted essays — never commit)
.corpus/

# local-only working directory
docs/marketer/
# bot (bot/) — secrets and local state
bot/.env
bot/*.sqlite3
bot/.venv/

# deploy secrets (server .env files, never commit)
ansible/secrets/
